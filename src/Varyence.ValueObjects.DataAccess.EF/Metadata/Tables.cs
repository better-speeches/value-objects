namespace Varyence.ValueObjects.DataAccess.EF.Metadata
{
    public sealed class Tables
    {
        public const string Person = nameof(Person);
        public const string Suffix = nameof(Suffix);
    }
}